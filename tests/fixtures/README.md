# Fixtures

The Claude Code stream was captured with a warm prompt cache from a recent CLI run, so its 4 uncached tokens and 42,419 cache reads are intentionally unrepresentative of a fresh-agent run.
